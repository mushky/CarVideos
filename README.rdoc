== README

Car Video Links

INSTALLATION:

After downloading or pulling the repository:

1) Ensure you have Ruby and Rails installed. You can check by typing ruby -v and rails -v respectively.

2) Open the directory you downloaded the application to.

3) On the command line run: bundle install

4) On the command line run: bin/rails s

5) In your browser go to the following url: localhost:3000